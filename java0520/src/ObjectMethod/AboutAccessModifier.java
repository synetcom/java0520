package ObjectMethod;

public class AboutAccessModifier {

	public static void main(String[] args) {
		/*
		 * private: this variable is only available inside the current class default:
		 * That one is available only inside the current package protected: That one is
		 * available only inside the current package and whenever the outside class that
		 * is child class then, it's protected public: open to all over the world
		 */

	}

}
