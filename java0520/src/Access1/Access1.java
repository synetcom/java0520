// if it's inside  a package should start package description
package Access1;

//public is an access modifier
public class Access1 {
//	doesn't have access modifier, that means default
//	if it's default, even though you extend the class, you can't 
//	access the variable from another package
	public int age = 25;
	public int height = 180;
}
